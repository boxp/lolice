apiVersion: v1
kind: Service
metadata:
  name: llama-server
  labels:
    app: llama-server
spec:
  type: ClusterIP
  selector:
    app: llama-server
  ports:
    - name: http
      port: 8080
      targetPort: http
    - name: metrics
      port: 9901
      targetPort: metrics
