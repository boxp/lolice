apiVersion: v1
kind: Service
metadata:
  name: openclaw
  namespace: openclaw
  labels:
    app.kubernetes.io/name: openclaw
    app.kubernetes.io/component: gateway
spec:
  selector:
    app.kubernetes.io/name: openclaw
  ports:
    - protocol: TCP
      port: 18789
      targetPort: 18789
