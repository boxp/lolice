apiVersion: v1
kind: Service
metadata:
  name: litellm
  namespace: openclaw
  labels:
    app: litellm
    app.kubernetes.io/name: openclaw
    app.kubernetes.io/component: litellm
spec:
  selector:
    app: litellm
  ports:
    - protocol: TCP
      port: 4000
      targetPort: 4000
