apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: ark-server-allow-all-egress
  namespace: ark-survival-ascended
spec:
  podSelector:
    matchLabels:
      app: ark-server
  policyTypes:
  - Egress
  egress:
  - {}  # Allow all egress traffic