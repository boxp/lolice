apiVersion: monitoring.coreos.com/v1
kind: Prometheus
metadata:
  name: k8s
  namespace: monitoring
spec:
  replicas: 1
  retention: 31d
  # アラートメール内の各アラートの Source リンク (generatorURL) の宛先。
  # 未設定だと Pod のアドレスになりリンクが機能しない。
  externalUrl: https://prometheus-web.b0xp.io
  tolerations:
    - key: lolice.io/gpu-worker
      operator: Equal
      value: "true"
      effect: NoSchedule
  enableFeatures:
    - otlp-write-receiver
  resources:
    requests:
      memory: 2Gi
      cpu: 500m
    limits:
      memory: 4Gi
  storage:
    volumeClaimTemplate:
      spec:
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: 60Gi
        storageClassName: longhorn
  scrapeConfigSelector:
    matchLabels:
      prometheus: system-monitoring-prometheus
  podMonitorSelector:
    matchLabels:
      release: prometheus
  # ServiceMonitor も明示的に opt-in したものだけを収集する。namespace に残った
  # Helm 管理外の Monitor が、所有者不明の TargetDown を発火させないためである。
  serviceMonitorSelector:
    matchLabels:
      release: prometheus
