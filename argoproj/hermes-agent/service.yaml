apiVersion: v1
kind: Service
metadata:
  name: hermes-agent
  labels:
    app: hermes-agent
spec:
  type: ClusterIP
  selector:
    app: hermes-agent
  ports:
    - name: api
      port: 8642
      targetPort: api
