apiVersion: v1
kind: Service
metadata:
  name: hermes-agent-dashboard
spec:
  type: ClusterIP
  selector:
    app: hermes-agent
  ports:
    - name: dashboard
      port: 9119
      targetPort: dashboard
