apiVersion: v1
kind: Service
metadata:
  name: codex-task-board-dashboard
  namespace: codex-workspace
spec:
  type: ClusterIP
  selector:
    app: codex-workspace
  ports:
    - name: dashboard
      port: 8080
      targetPort: dashboard
